import App from './App'
import React from 'react'
import ReactDOM from 'react-dom'

describe('<App />', () => {
	it('renders <App /> withour error', () =>{
		// const div = document.createElement('div')
		// ReactDOM.render(<App />, div)
	})
})